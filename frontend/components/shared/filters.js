import { Search } from "lucide-react";import { Input } from "@/components/ui/input";import { Select } from "@/components/ui/select";
export function Filters({search,onSearch,children,placeholder='Search records...'}){return <div className="mb-5 flex flex-col gap-3 md:flex-row md:items-center md:justify-between"><div className="relative w-full md:max-w-sm"><Search className="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-slate-400"/><Input value={search||''} onChange={e=>onSearch?.(e.target.value)} placeholder={placeholder} className="pl-9"/></div><div className="flex flex-wrap gap-2">{children}</div></div>}
export function FilterSelect({value,onChange,children}){return <Select value={value} onChange={e=>onChange(e.target.value)} className="w-auto min-w-36">{children}</Select>}
