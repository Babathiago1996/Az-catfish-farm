import { Button } from "@/components/ui/button";
export function PageHeader({eyebrow,title,description,action,secondary}){return <div className="mb-7 flex flex-col justify-between gap-4 md:flex-row md:items-end"><div>{eyebrow&&<div className="text-[11px] font-bold uppercase tracking-[.18em] text-blue-600">{eyebrow}</div>}<h2 className="mt-1 text-3xl font-black tracking-tight">{title}</h2>{description&&<p className="mt-2 max-w-2xl text-sm leading-6 text-[var(--muted)]">{description}</p>}</div>{(action||secondary)&&<div className="flex flex-wrap gap-2">{secondary}{action&&<Button onClick={action.onClick} variant={action.variant||'default'}>{action.icon}{action.label}</Button>}</div>}</div>}
