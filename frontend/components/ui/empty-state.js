import { Inbox } from "lucide-react";
export function EmptyState({ title="Nothing here yet", description="There are no records matching your current filters.", action }) { return <div className="flex flex-col items-center justify-center rounded-2xl border border-dashed border-[var(--border)] bg-[var(--card)] px-6 py-14 text-center"><div className="mb-4 rounded-2xl bg-[var(--accent)] p-3 text-[var(--primary)]"><Inbox className="h-6 w-6" /></div><h3 className="font-bold">{title}</h3><p className="mt-1 max-w-md text-sm text-[var(--muted)]">{description}</p>{action && <div className="mt-5">{action}</div>}</div>; }
