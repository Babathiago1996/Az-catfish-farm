"use client";
import Link from "next/link";import {Suspense,useState} from "react";import {useSearchParams,useRouter} from "next/navigation";import {useForm} from "react-hook-form";import {zodResolver} from "@hookform/resolvers/zod";import {z} from "zod";import {ArrowLeft,LockKeyhole} from "lucide-react";import {api} from "@/lib/api";import {Button} from "@/components/ui/button";import {Input} from "@/components/ui/input";import {Label} from "@/components/ui/label";import {toast} from "sonner";
const schema=z.object({password:z.string().min(8).max(128),confirmPassword:z.string().min(8)}).refine(x=>x.password===x.confirmPassword,{path:['confirmPassword'],message:'Passwords do not match.'});
function ResetForm(){const params=useSearchParams();const router=useRouter();const token=params.get('token')||'';const [done,setDone]=useState(false);const {register,handleSubmit,formState:{errors,isSubmitting}}=useForm({resolver:zodResolver(schema)});const submit=async(d)=>{if(!token){toast.error('Reset token is missing.');return}try{await api.auth.resetPassword({token,password:d.password,confirmPassword:d.confirmPassword});setDone(true);toast.success('Password reset successful.');setTimeout(()=>router.replace('/login'),1200)}catch(e){toast.error(e.message)}};return <div className="w-full max-w-md rounded-3xl border bg-[var(--card)] p-7 shadow-xl sm:p-9"><Link href="/login" className="inline-flex items-center gap-2 text-sm font-semibold text-slate-500"><ArrowLeft className="h-4 w-4"/> Back to sign in</Link><div className="mt-8 grid h-12 w-12 place-items-center rounded-2xl bg-blue-50 text-blue-600"><LockKeyhole/></div><h1 className="mt-6 text-3xl font-black">Create a new password</h1>{done?<div className="mt-6 rounded-2xl bg-emerald-50 p-4 text-sm text-emerald-800">Password reset complete. Redirecting you to sign in…</div>:<form onSubmit={handleSubmit(submit)} className="mt-7 space-y-5"><div><Label htmlFor="password" required>New password</Label><Input id="password" type="password" {...register('password')}/>{errors.password&&<p className="mt-1 text-xs text-red-600">Password must contain between 8 and 128 characters.</p>}</div><div><Label htmlFor="confirmPassword" required>Confirm password</Label><Input id="confirmPassword" type="password" {...register('confirmPassword')}/>{errors.confirmPassword&&<p className="mt-1 text-xs text-red-600">{errors.confirmPassword.message}</p>}</div><Button className="w-full" disabled={isSubmitting}>{isSubmitting?'Resetting…':'Reset password'}</Button></form>}</div>}
export default function Reset(){return <main className="grid min-h-screen place-items-center bg-[var(--background)] p-5"><Suspense fallback={<div className="text-sm text-slate-400">Loading reset form…</div>}><ResetForm/></Suspense></main>}
