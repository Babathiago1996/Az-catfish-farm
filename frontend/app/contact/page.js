"use client";
import { useEffect,useState } from "react";import { PublicShell } from "@/components/layout/public-shell";import { api } from "@/lib/api";import { Mail,Phone,MapPin,MessageCircle } from "lucide-react";
export default function Contact(){const [data,setData]=useState(null);useEffect(()=>{api.public.contact().then(r=>setData(r?.contact)).catch(()=>{})},[]);return <PublicShell><section className="mesh-bg"><div className="mx-auto max-w-7xl px-4 py-20 sm:px-6 lg:px-8"><div className="max-w-2xl"><div className="text-xs font-bold uppercase tracking-[.2em] text-blue-600">Contact</div><h1 className="mt-3 text-5xl font-black tracking-tight">Let’s talk fish, supply and farm services.</h1><p className="mt-5 text-lg leading-8 text-[var(--muted)]">Reach the farm directly using the details below.</p></div><div className="mt-12 grid gap-5 md:grid-cols-3">{[[Phone,'Phone',data?.phone, data?.phone?`tel:${data.phone}`:null],[Mail,'Email',data?.email,data?.email?`mailto:${data.email}`:null],[MapPin,'Farm address',data?.address||'Lagos, Nigeria',null]].map(([Icon,title,value,href])=><div key={title} className="rounded-3xl border bg-[var(--card)] p-7 shadow-sm"><div className="grid h-12 w-12 place-items-center rounded-2xl bg-blue-50 text-blue-600 dark:bg-blue-950/40"><Icon/></div><h3 className="mt-5 font-black">{title}</h3>{href?<a href={href} className="mt-2 block text-sm font-semibold text-blue-600">{value}</a>:<p className="mt-2 text-sm text-[var(--muted)]">{value}</p>}</div>)}</div><div className="mt-10 rounded-3xl border bg-[var(--card)] p-7"><div className="flex items-center gap-3"><MessageCircle className="text-emerald-600"/><div><div className="font-black">Prefer WhatsApp?</div><div className="text-sm text-[var(--muted)]">If the farm has a WhatsApp link configured, use it from the social links.</div></div></div></div></div></section></PublicShell>}
