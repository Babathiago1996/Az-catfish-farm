"use client";
import { useEffect,useState } from "react";import { PublicShell } from "@/components/layout/public-shell";import { api } from "@/lib/api";import { formatNumber } from "@/lib/utils";import { Fish,Waves,Scale } from "lucide-react";
export default function Overview(){const [o,setO]=useState(null);useEffect(()=>{api.public.overview().then(r=>setO(r?.overview)).catch(()=>{})},[]);return <PublicShell><section className="mesh-bg"><div className="mx-auto max-w-7xl px-4 py-20 sm:px-6 lg:px-8"><div className="max-w-2xl"><div className="text-xs font-bold uppercase tracking-[.2em] text-blue-600">Farm overview</div><h1 className="mt-3 text-5xl font-black tracking-tight">A live look at active production.</h1><p className="mt-5 text-lg leading-8 text-[var(--muted)]">Public production figures are generated directly from active ponds.</p></div><div className="mt-12 grid gap-5 md:grid-cols-3">{[[Waves,'Active ponds',o?.totalActivePonds||0],[Fish,'Fish in production',o?.totalFishCount||0],[Scale,'Estimated biomass',`${formatNumber(o?.totalBiomassKg||0,3)} kg`]].map(([Icon,t,v])=><div key={t} className="rounded-3xl border bg-[var(--card)] p-7 shadow-sm"><div className="grid h-12 w-12 place-items-center rounded-2xl bg-blue-50 text-blue-600 dark:bg-blue-950/40"><Icon/></div><div className="mt-6 text-4xl font-black">{typeof v==='number'?formatNumber(v):v}</div><div className="mt-1 text-sm text-[var(--muted)]">{t}</div></div>)}</div></div></section></PublicShell>}
