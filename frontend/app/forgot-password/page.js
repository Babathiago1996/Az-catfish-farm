"use client";
import Link from "next/link";import {useForm} from "react-hook-form";import {zodResolver} from "@hookform/resolvers/zod";import {z} from "zod";import {useState} from "react";import {ArrowLeft,Mail} from "lucide-react";import {api} from "@/lib/api";import {Button} from "@/components/ui/button";import {Input} from "@/components/ui/input";import {Label} from "@/components/ui/label";import {Spinner} from "@/components/ui/spinner";import {toast} from "sonner";
const schema=z.object({email:z.string().email('Enter a valid email address.')});
export default function Forgot(){const [sent,setSent]=useState(false);const {register,handleSubmit,formState:{errors,isSubmitting}}=useForm({resolver:zodResolver(schema)});const submit=async(d)=>{try{await api.auth.forgotPassword(d);setSent(true);toast.success('If the account exists, a reset message has been sent.')}catch(e){toast.error(e.message)}};return <main className="grid min-h-screen place-items-center bg-[var(--background)] p-5"><div className="w-full max-w-md rounded-3xl border bg-[var(--card)] p-7 shadow-xl sm:p-9"><Link href="/login" className="inline-flex items-center gap-2 text-sm font-semibold text-slate-500"><ArrowLeft className="h-4 w-4"/> Back to sign in</Link><div className="mt-8 grid h-12 w-12 place-items-center rounded-2xl bg-blue-50 text-blue-600"><Mail/></div><h1 className="mt-6 text-3xl font-black">Reset your password</h1><p className="mt-2 text-sm leading-6 text-[var(--muted)]">Enter the administrator email. The backend deliberately keeps account existence private.</p>{sent?<div className="mt-6 rounded-2xl bg-emerald-50 p-4 text-sm leading-6 text-emerald-800">If an administrator account exists for that email, a password reset message has been sent.</div>:<form onSubmit={handleSubmit(submit)} className="mt-7 space-y-5"><div><Label htmlFor="email" required>Email</Label><Input id="email" type="email" {...register('email')}/>{errors.email&&<p className="mt-1 text-xs text-red-600">{errors.email.message}</p>}</div><Button className="w-full" disabled={isSubmitting}>{isSubmitting?<Spinner/>:'Send reset instructions'}</Button></form>}<Link href="/login" className="mt-6 block text-center text-sm font-bold text-blue-600">Return to login</Link></div></main>}
