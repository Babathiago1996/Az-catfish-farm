"use client";
import {useEffect,useState} from "react";import {Plus,Fish,CalendarDays} from "lucide-react";import {AdminLayout} from "@/components/shared/admin-layout";import {PageHeader} from "@/components/shared/page-header";import {Card} from "@/components/ui/card";import {Button} from "@/components/ui/button";import {Input} from "@/components/ui/input";import {Select} from "@/components/ui/select";import {Dialog} from "@/components/ui/dialog";import {Label} from "@/components/ui/label";import {Table,THead,TBody,TR,TH,TD} from "@/components/ui/table";import {Pagination} from "@/components/shared/pagination";import {api} from "@/lib/api";import {formatDate,formatCurrency,formatNumber,pondName,toInputDate} from "@/lib/utils";import {toast} from "sonner";import {useForm} from "react-hook-form";
export default function Stocking(){const [ponds,setPonds]=useState([]),[rows,setRows]=useState([]),[pagination,setPagination]=useState({}),[pond,setPond]=useState(''),[page,setPage]=useState(1),[open,setOpen]=useState(false);const load=()=>api.stocking.list({page,limit:20,pond}).then(r=>{setRows(r?.records||[]);setPagination(r?.pagination||{})}).catch(e=>toast.error(e.message));useEffect(()=>{api.ponds.list({limit:100}).then(r=>setPonds(r?.ponds||[])).catch(()=>{})},[]);useEffect(() => {
    load();
  }, [page,pond]);const {register,handleSubmit,reset,formState:{isSubmitting}}=useForm({defaultValues:{stockingDate:toInputDate(),fingerlingQuantity:1000,fingerlingSize:3,fingerlingSizeUnit:'cm',cost:0,expectedHarvestDate:'',initialWeight:0,supplier:'',notes:'',pond:''}});const submit=async(d)=>{try{await api.stocking.create({...d,fingerlingQuantity:Number(d.fingerlingQuantity),fingerlingSize:Number(d.fingerlingSize),cost:Number(d.cost),initialWeight:Number(d.initialWeight),pond:d.pond});toast.success('Stocking recorded and pond count updated.');setOpen(false);reset();load()}catch(e){toast.error(e.message)}};return <AdminLayout title="Stocking" description="Introduce fingerlings into ponds and track production starts"><PageHeader eyebrow="Production" title="Stocking" description="Every stocking event becomes part of your production history." action={{label:'Record stocking',icon:<Plus className="h-4 w-4"/>,onClick:()=>setOpen(true)}}/><Card className="p-5"><div className="mb-5 flex justify-end"><Select value={pond} onChange={e=>{setPage(1);setPond(e.target.value)}} className="w-full md:w-56"><option value="">All ponds</option>{ponds.map(p=><option key={p._id} value={p._id}>{p.name} · #{p.pondNumber}</option>)}</Select></div>{rows.length?<Table><THead><TR><TH>Date</TH><TH>Pond</TH><TH>Fingerlings</TH><TH>Size</TH><TH>Cost</TH><TH>Expected harvest</TH></TR></THead><TBody>{rows.map(r=><TR key={r._id}><TD><div className="flex items-center gap-2"><CalendarDays className="h-4 w-4 text-slate-400"/>{formatDate(r.stockingDate)}</div></TD><TD className="font-bold">{pondName(r.pond)}</TD><TD><div className="flex items-center gap-2"><Fish className="h-4 w-4 text-blue-600"/>{formatNumber(r.fingerlingQuantity)}</div></TD><TD>{r.fingerlingSize} {r.fingerlingSizeUnit}</TD><TD>{formatCurrency(r.cost)}</TD><TD>{formatDate(r.expectedHarvestDate)}</TD></TR>)}</TBody></Table>:<div className="py-14 text-center text-sm text-[var(--muted)]">No stocking records yet.</div>}<Pagination page={pagination.page} pages={pagination.pages} onChange={setPage}/></Card><Dialog open={open} onOpenChange={setOpen} title="Record stocking" description="The backend will increase the selected pond's current fish count."><form onSubmit={handleSubmit(submit)} className="grid gap-5 sm:grid-cols-2"><div><Label required>Pond</Label><Select {...register('pond')}><option value="">Select pond</option>{ponds.map(p=><option key={p._id} value={p._id}>{p.name} · #{p.pondNumber}</option>)}</Select></div><div><Label required>Stocking date</Label><Input type="date" {...register('stockingDate')}/></div><div><Label required>Fingerling quantity</Label><Input type="number" min="1" {...register('fingerlingQuantity')}/></div><div><Label>Fingerling size</Label><div className="flex gap-2"><Input type="number" step="0.01" {...register('fingerlingSize')}/><Select {...register('fingerlingSizeUnit')} className="w-28"><option>cm</option><option>inch</option><option>gram</option></Select></div></div><div><Label>Cost</Label><Input type="number" step="0.01" min="0" {...register('cost')}/></div><div><Label>Initial average weight (g)</Label><Input type="number" step="0.01" min="0" {...register('initialWeight')}/></div><div><Label>Expected harvest date</Label><Input type="date" {...register('expectedHarvestDate')}/></div><div><Label>Supplier</Label><Input {...register('supplier')}/></div><div className="sm:col-span-2"><Label>Notes</Label><textarea {...register('notes')} className="min-h-24 w-full rounded-xl border bg-transparent p-3 text-sm outline-none"/></div><div className="flex justify-end gap-2 sm:col-span-2"><Button type="button" variant="outline" onClick={()=>setOpen(false)}>Cancel</Button><Button type="submit" disabled={isSubmitting}>{isSubmitting?'Saving…':'Record stocking'}</Button></div></form></Dialog></AdminLayout>}
