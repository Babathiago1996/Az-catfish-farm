"use client";
import {useEffect,useState} from "react";import {Plus,HeartPulse,AlertTriangle} from "lucide-react";import {AdminLayout} from "@/components/shared/admin-layout";import {PageHeader} from "@/components/shared/page-header";import {MetricCard} from "@/components/shared/metric-card";import {Card} from "@/components/ui/card";import {Button} from "@/components/ui/button";import {Input} from "@/components/ui/input";import {Select} from "@/components/ui/select";import {Dialog} from "@/components/ui/dialog";import {Label} from "@/components/ui/label";import {Table,THead,TBody,TR,TH,TD} from "@/components/ui/table";import {Pagination} from "@/components/shared/pagination";import {api} from "@/lib/api";import {MORTALITY_CAUSES} from "@/lib/constants";import {formatDate,formatNumber,pondName,toInputDate,labelize} from "@/lib/utils";import {toast} from "sonner";import {useForm} from "react-hook-form";
export default function Mortality(){const [rows,setRows]=useState([]),[ponds,setPonds]=useState([]),[summary,setSummary]=useState(null),[pagination,setPagination]=useState({}),[page,setPage]=useState(1),[pond,setPond]=useState(''),[open,setOpen]=useState(false);const load=()=>Promise.all([api.mortality.list({page,limit:30,pond}),api.mortality.summary({pond})]).then(([r,s])=>{setRows(r?.records||[]);setPagination(r?.pagination||{});setSummary(s)}).catch(e=>toast.error(e.message));useEffect(()=>{api.ponds.list({limit:100}).then(r=>setPonds(r?.ponds||[])).catch(()=>{})},[]);useEffect(() => {
    load();
  }, [page,pond]);const {register,handleSubmit,reset}=useForm({defaultValues:{date:toInputDate(),pond:'',quantity:1,estimatedCause:'unknown',notes:'',imageUrl:'',imagePublicId:''}});const submit=async(d)=>{try{await api.mortality.create({...d,quantity:Number(d.quantity)});toast.success('Mortality recorded and pond count synchronized.');setOpen(false);reset();load()}catch(e){toast.error(e.message)}};return <AdminLayout title="Mortality" description="Monitor fish losses and keep pond counts synchronized"><PageHeader eyebrow="Fish health" title="Mortality" description="Record losses carefully and understand causes by pond." action={{label:'Record mortality',icon:<Plus className="h-4 w-4"/>,onClick:()=>setOpen(true)}}/><div className="grid gap-4 sm:grid-cols-3"><MetricCard label="Mortality" value={formatNumber(summary?.totalMortality)} sub="selected view" icon={HeartPulse}/><MetricCard label="Records" value={formatNumber(summary?.records)} sub="mortality events" icon={AlertTriangle}/><MetricCard label="Top cause" value={labelize(summary?.byCause?.[0]?.cause||'—')} sub={`${formatNumber(summary?.byCause?.[0]?.quantity)} fish`} icon={HeartPulse}/></div><Card className="mt-5 p-5"><div className="mb-5 flex justify-end"><Select value={pond} onChange={e=>{setPage(1);setPond(e.target.value)}} className="w-full md:w-56"><option value="">All ponds</option>{ponds.map(p=><option key={p._id} value={p._id}>{p.name} · #{p.pondNumber}</option>)}</Select></div>{rows.length?<Table><THead><TR><TH>Date</TH><TH>Pond</TH><TH>Quantity</TH><TH>Cause</TH><TH>Notes</TH></TR></THead><TBody>{rows.map(r=><TR key={r._id}><TD>{formatDate(r.date)}</TD><TD className="font-bold">{pondName(r.pond)}</TD><TD className="font-black text-red-600">{formatNumber(r.quantity)}</TD><TD>{labelize(r.estimatedCause)}</TD><TD className="max-w-xs truncate text-[var(--muted)]">{r.notes||'—'}</TD></TR>)}</TBody></Table>:<div className="py-14 text-center text-sm text-[var(--muted)]">No mortality records found.</div>}<Pagination page={pagination.page} pages={pagination.pages} onChange={setPage}/></Card><Dialog open={open} onOpenChange={setOpen} title="Record mortality" description="The backend prevents recording more fish than calculated available stock."><form onSubmit={handleSubmit(submit)} className="grid gap-5 sm:grid-cols-2"><div><Label required>Date</Label><Input type="date" {...register('date')}/></div><div><Label required>Pond</Label><Select {...register('pond')}><option value="">Select pond</option>{ponds.map(p=><option key={p._id} value={p._id}>{p.name} · #{p.pondNumber}</option>)}</Select></div><div><Label required>Quantity</Label><Input type="number" min="1" {...register('quantity')}/></div><div><Label>Estimated cause</Label><Select {...register('estimatedCause')}>{MORTALITY_CAUSES.map(x=><option key={x}>{x}</option>)}</Select></div><div className="sm:col-span-2"><Label>Notes</Label><textarea {...register('notes')} className="min-h-24 w-full rounded-xl border bg-transparent p-3 text-sm"/></div><div><Label>Image URL</Label><Input {...register('imageUrl')} placeholder="Optional Cloudinary URL"/></div><div><Label>Image public ID</Label><Input {...register('imagePublicId')}/></div><div className="flex justify-end gap-2 sm:col-span-2"><Button type="button" variant="outline" onClick={()=>setOpen(false)}>Cancel</Button><Button type="submit">Record mortality</Button></div></form></Dialog></AdminLayout>}
